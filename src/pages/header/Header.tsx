import {FC} from 'react';
import ResponsiveAppBar from "../../components/ResponsiveAppBar";

const Header: FC<{}> = ({}) => {
    return (
        <ResponsiveAppBar/>
    );
};

export default Header;
